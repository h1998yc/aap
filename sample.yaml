---
- name: create group
  hosts: webdb
  tasks:
  - name: using group module
    group:
      name: oracle
      state: present
      gid: 2400

  - name: using user module
    ansible.builtin.user:
      name: mini
      uid: 3400
      group: oracle
